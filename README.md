# Clearly Audacious legacy redirect

Static redirect host for `clearlyaudacious.robertochavarria.com` after the production site moves to `https://clearlyaudacious.com`.

The `CNAME` file and custom-domain assignment are intentionally added only after the main site releases the legacy hostname.
